// External includes.

// Standard includes.

// Internal includes.
use super::HasPosition;

/// Defines a something that has been [`Placed`](trait.Placed.html) at a [`Position`](struct.Position.html).
pub trait Placed: HasPosition {}
